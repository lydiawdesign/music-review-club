<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Music Review Club!</title>

  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Lobster&display=swap" rel="stylesheet">
  <!-- CSS only -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">
  <link rel="stylesheet" href="/css/style.css">
</head>

<body>

 <div class="container">
   <header>
      <nav class="navbar navbar-expand-sm navbar-light fixed-top" id="navbar">
        <a class="navbar-brand">
          <span class="hidden fw-bold fs-1 ps-5" id="header">Music Review Club</span>
        </a>

        <button 
            class="navbar-toggler pe-4" 
            type="button" 
            data-bs-toggle="collapse" 
            data-bs-target="#toggleMobileMenu" 
            aria-controls="toggleMobileMenu" 
            aria-expanded="false" 
            aria-label="Toggle navigation"
          >
          <span class="navbar-toggler-icon"></span>
         </button>

        <div class="collapse navbar-collapse pe-4" id="toggleMobileMenu">
        
          <ul class="navbar-nav ms-auto text-center">
            {{#if loggedIn}}
            <li>
              <button class="nav-link fw-bold text-uppercase text-white buttonStyle">Feed</button>
            </li>
            <li>
              <button class="nav-link fw-bold text-uppercase text-white buttonStyle">New Post</button>
            </li>
            <li>
              <button class="nav-link fw-bold text-uppercase text-white buttonStyle" id="logout">Logout</button>
            </li>
            {{else}}
            <li>
              <button class="nav-link fw-bold text-uppercase text-white buttonStyle">Log In</button>
            </li>
            {{/if}}
                
          </ul>
        </div>
      </nav>
  
    </header>
    
    <main class="container container-fluid mt-5">
      {{{ body }}}
    </main>
    <footer>
        
    </footer>

    {{#if loggedIn}}
    <script src="/js/logout.js"></script>
    {{/if}}

  </body>

</html>